<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= task.titre %></title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="vh-100 m-0">
    <div class="container p-5 h-100">
        <div  class="card p-5 h-100">
            <h1 class="card-title"><%= task.titre %></h1>
            <p class="card-text"><%= task.description %></p>
            <form action="/tasks/<%= task.id %>/status" method="post" class="d-inline">
                <button type="submit" class="btn btn-sm <%= task.compeleter ? 'btn-success' : 'btn-danger' %> w-25">
                    <%= task.compeleter ? 'complétée' : 'non complétée' %>
                </button>
            </form>
            <div class="d-flex justify-content-around mt-3">
                <form action="/tasks/<%= task.id %>/edit" method="get" class="w-100 pr-1">
                    <button type="submit" class="btn btn-sm btn-info w-100">modifier</button>
                </form>
                <form action="/tasks/<%= task.id %>/delete" method="post" class="w-100 pl-1">
                    <button type="submit" class="btn btn-sm btn-danger w-100">Supprimer</button>
                </form>
            </div>
        </div>
        <a href="/tasks">Retour à la liste des tâches</a>
    </div>
</body>
</html>